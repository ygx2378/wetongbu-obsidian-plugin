# WeTongbu 0.9.6

Community review compatibility release.

- Uses Obsidian's `Platform` API instead of the browser `navigator` API when naming a device.
- Keeps all task processing, account authorization, storage, and Vault behavior unchanged from 0.9.5.

Release assets: `main.js` and `manifest.json`.
