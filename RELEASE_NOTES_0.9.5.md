# WeTongbu 0.9.5

Community review hardening release.

- Uses a directory-compliant manifest description and a bilingual README with clear network, account, payment, privacy, and Vault-access disclosures.
- Removes obsolete legacy R2 inbox migration code and its AWS SDK dependency from the release bundle.
- Removes system clipboard access; recovery codes remain visible in the plugin settings for manual copying.
- Keeps the current account authorization, local-by-default attachments, optional Pro hosted image links, and task-package validation.

Release assets: `main.js` and `manifest.json`.
