# 微同步 0.10.4

## 修复首次多端同步超过 50% 的问题

- 首次同步时，如果本机和电脑/云端都有内容，先让用户选择同步方向，避免误覆盖。
- 支持选择“从电脑/云端下载到本机”或“从本机上传到电脑/云端”。
- 首次同步按明确方向执行，不再被 50% 安全阈值误拦截。
- 空的失败状态文件可自动恢复，不会阻塞首次重试。
- “微同步”入口统一执行文章任务和 Vault 多端同步，减少“没有待同步文章”的误导提示。
