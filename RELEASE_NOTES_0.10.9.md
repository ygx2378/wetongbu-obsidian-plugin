# 微同步 0.10.9

## 修复腾讯云 COS 多端同步读取失败

- 修正腾讯云 COS 的虚拟主机访问地址，避免移动端读取同步对象时返回 HTTP 403。
- 修正移动端签名请求的 Host 处理，兼容 Obsidian iOS/Android 的网络请求实现。
- 保留 Free 用户自有对象存储和 Pro 托管对象存储的既有同步流程。

升级后无需重新填写对象存储密钥；请在手机端重新加载插件后再次点击“立即同步”。
